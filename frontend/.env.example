# Leave empty for Vite proxy (recommended locally). Setting an absolute API URL
# can break SPA calls if the API does not answer CORS preflight.
VITE_API_BASE_URL=

# Clerk publishable key from https://dashboard.clerk.com → API Keys
VITE_CLERK_PUBLISHABLE_KEY=pk_test_...
